/**  OPERADORES ARITMETICOS 
 * + Adição / Concatenação
 * - Subtração
 * / Divisão
 * * Multiplicação
 * ** Potenciação
 * % Resto da divisão
 * Incremento = ++ (Pré e Pró)
 * Decremento = -- (Pré e Pró)
 * 
 * Operadores de atribuição
 * += -= *= /=
 * 
 */

//let contador = 1;
//contador++;
//console.log(contador);

const passo = 50;
let contador = 0;
contador += passo; // contador = contador + 50;